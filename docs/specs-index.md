# crustyimg — spec index

> **Generated by `just specs-index` — do not hand-edit, regenerate instead.** Source of
> truth is each spec's front-matter (`task.*`, `project.*`, `value_link`) plus its
> `# SPEC-NNN: <title>` heading. Scope: every `SPEC-*.md` under `projects/*/specs/`
> (in-flight and the `specs/done/` archive) — not build/verify prompts, not timelines,
> not patches (DEC-043; see `just specs-by-stage` for those).
>
> For what each CLI verb does, see [`cli-reference.md`](cli-reference.md). For live
> cycle/cost state rather than this static snapshot, use `just status` (active project)
> or `just specs-by-stage` (cost + ship dates, every project).

**118 specs across 7 projects** (114 shipped, 4 in flight).

## Contents

- [PROJ-001 — crustyimg MVP — clean rebuild](#proj-001) — shipped · 45 specs
- [PROJ-002 — the optimization engine core](#proj-002) — shipped · 4 specs
- [PROJ-004 — image lint — "clippy for image assets"](#proj-004) — shipped · 6 specs
- [PROJ-007 — Reproducible build (build + cache + lockfile + `--watch`)](#proj-007) — shipped · 9 specs
- [PROJ-008 — WASM core + demo page](#proj-008) — shipped · 33 specs
- [PROJ-009 — Input reach (modern-format decode)](#proj-009) — shipped · 4 specs
- [PROJ-010 — Post-launch correctness and consolidation](#proj-010) — active · 17 specs

---

## <a id="proj-001"></a>PROJ-001 — crustyimg MVP — clean rebuild

*status: shipped · 45 specs, 45 shipped*

A single image model fronted by a pluggable Operation pipeline lets a user tune an edit once on one image and replay the exact same recipe across many images — making routine image prep (especially for the web) faster…

### STAGE-001 — foundation and pipeline core _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-001](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-001-cargo-project-and-multi-os-ci.md) | medium | **shipped** | cargo project and multi-OS CI | infrastructure enabling STAGE-001's runnable binary + green multi-OS CI gate that every later spec builds and tests against |
| [SPEC-002](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-002-canonical-image-type-and-load.md) | medium | **shipped** | canonical Image type and load | infrastructure enabling the canonical image model the whole pipeline and metadata lane build on |
| [SPEC-003](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-003-operation-trait-and-pipeline.md) | high | **shipped** | Operation trait and Pipeline | infrastructure: the pipeline engine + Operation extension point every transform/recipe plugs into |
| [SPEC-004](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-004-source-input-abstraction.md) | medium | **shipped** | Source input abstraction | infrastructure: the Source abstraction that turns one CLI argument into the ordered, deterministic, safe list of inputs every later command and batch run iterates over |
| [SPEC-005](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-005-sink-output-abstraction.md) | medium | **shipped** | Sink output abstraction | Infrastructure enabling STAGE-001's "decode once -> ops -> emit to a sink" keystone — the output half of the pipeline (file / dir+template / stdout / terminal display), with the output path-traversal + overwrite hardening the project… |
| [SPEC-006](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-006-recipe-toml-and-operation-registry.md) | medium | **shipped** | Recipe TOML and operation registry | This is the heart of the project thesis "tune once, replay across many": it makes an operation chain serializable to TOML and reconstructable through a registry, so the exact same recipe can later (STAGE-005) replay unchanged across one… |
| [SPEC-007](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-007-clap-cli-skeleton-and-dispatch.md) | medium | **shipped** | clap CLI skeleton and dispatch | Turns the shipped library (Image/Operation/Pipeline/Source/Sink/Recipe) into a usable `crustyimg` binary with a real subcommand interface and dispatch — completing STAGE-001's 'a runnable binary with a real subcommand interface'. |

### STAGE-002 — view and info _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-008](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-008-view-command-terminal-display.md) | medium | **shipped** | view command terminal display | Delivers STAGE-002's `view` capability: the first real command that renders an image through the display Sink, proving the source → load → sink path end to end. |
| [SPEC-009](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-009-info-command-image-inspection.md) | medium | **shipped** | info command image inspection | Delivers STAGE-002's `info` capability — structured, read-only image inspection (dimensions/format/file-size/color-type/bit-depth/alpha/ICC+EXIF presence), EXIF tag dump, and the machine-readable `--json` output convention later commands… |

### STAGE-003 — transform and output _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-010](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-010-resize-operation-and-operation-params-mechanism.md) | medium | **shipped** | resize operation and operation params mechanism | Delivers STAGE-003's first pixel-geometry Operation (`resize`, all six modes on the fast_image_resize SIMD backend) AND the first parameterized-operation params mechanism (DEC-014) + registry wiring + recipe round-trip — recipe-usable with… |
| [SPEC-011](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-011-resize-cli-command-and-multi-input-fan-out.md) | medium | **shipped** | resize cli command and multi input fan-out | Wires the user-facing `resize` command on top of SPEC-010's Resize Operation — six mode flags, single-input `-o`/`-o -`, and SEQUENTIAL multi-input `--out-dir` fan-out with source-format preservation and partial-batch (exit 6) semantics —… |
| [SPEC-012](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-012-thumbnail-command-bounded-resize-and-square-crop.md) | medium | **shipped** | thumbnail command bounded resize and square crop | Adds the `thumbnail` convenience command — a bounded small resize (default long-edge cap) with `--square` center-crop — by mapping `(size, square)` onto the SHIPPED `Resize` op (`--square` ≡ resize `fill` NxN; plain ≡ resize `max` N) and… |
| [SPEC-013](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-013-shrink-command-web-prep-resize-quality-encode-strip.md) | medium | **shipped** | shrink command — web-prep resize + quality encode + strip | Delivers STAGE-003's headline web-prep command — `shrink` makes 'optimize this image for the web' one short command (resize to a sane bound + real quality-aware JPEG encode + metadata drop), and wires the `-q/--quality` knob (DEC-016) that… |
| [SPEC-014](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-014-convert-command-re-encode-across-core-formats.md) | medium | **shipped** | convert command — re-encode across core formats | Delivers STAGE-003's `convert` command — re-encode an image (or a batch) from one core format to another in one short command, the everyday 'I need this PNG as a JPEG' task, reusing the fan-out + the DEC-016 quality knob. |
| [SPEC-015](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-015-auto-orient-command-and-operation-bake-exif-orientation-into-pixels.md) | medium | **shipped** | auto-orient command and operation — bake EXIF orientation into pixels | Delivers STAGE-003's `auto-orient` — bakes a photo's EXIF orientation into pixels and clears the tag, fixing the most common silent-rotation bug, and is the first recipe-usable Operation that reads container metadata to drive a pixel… |

### STAGE-004 — compose and metadata _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-026](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-026-metadata-lane-strip-and-clean-gps.md) | high | **shipped** | metadata lane v1 — `strip` + `clean --gps` | Opens the container-level metadata lane (DEC-003) with `strip` + `clean --gps` — the privacy half of STAGE-004 and the start of the project's verifiable-privacy axis. |
| [SPEC-027](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-027-set-exif-tags-artist-copyright-description.md) | high | **shipped** | `set` — write EXIF artist/copyright/description | Adds container-lane `set` (write artist/copyright/description) — the attribution counterpart to SPEC-026's strip/clean, continuing STAGE-004's metadata lane with no pixel re-encode. |
| [SPEC-028](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-028-copy-metadata-from-one-image-to-another.md) | high | **shipped** | `copy-metadata` — transfer EXIF + ICC from one image to another | Completes the container-lane metadata commands with `copy-metadata` (transfer EXIF+ICC SRC→DST), the last metadata piece of STAGE-004. |
| [SPEC-029](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-029-watermark-image-overlay-at-gravity-anchor.md) | high | **shipped** | `watermark` — image overlay at a gravity anchor | Adds `watermark` — the image-overlay compositing command and the first multi-image Operation — the compositing half of STAGE-004. |
| [SPEC-030](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-030-text-watermark-render-text-at-gravity-anchor.md) | high | **shipped** | text watermark render text at gravity anchor | Adds text mode to `watermark` (render a string at a gravity anchor) — the last STAGE-004 command; completes the compose-and-metadata stage. |

### STAGE-005 — batch and recipes _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-031](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-031-parallel-batch-apply-recipe-over-a-source-list.md) | high | **shipped** | parallel batch `apply --recipe` over a source list | Turns `apply --recipe` into the parallel batch replay that IS the project thesis — tune once, replay the same recipe across a whole directory. |
| [SPEC-032](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-032-edit-one-shot-multi-op-and-save-recipe.md) | high | **shipped** | edit one-shot multi-op and save-recipe | Completes the project thesis — `edit` tunes an ordered op chain on one image and `--save-recipe` captures it as the exact TOML that `apply` replays across a directory: tune once → save → replay. |

### STAGE-006 — hardening and security assessment _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-033](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-033-decode-resource-limits-on-image-load.md) | high | **shipped** | decode resource limits on image load | First STAGE-006 hardening item: bounds decoder dimensions/allocation on the one canonical load path so an untrusted file can't trigger a decompression bomb — closing a known gap before the MVP ships. |
| [SPEC-034](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-034-path-and-symlink-traversal-hardening-across-source-and-sink.md) | high | **shipped** | path and symlink traversal hardening across source and sink | Second STAGE-006 hardening item: closes the write-through-symlink (Sink) and glob-escape-bypass (Source) traversal gaps so an untrusted directory/glob or a planted symlink can't redirect reads or writes outside the target tree. |
| [SPEC-035](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-035-security-grade-recipe-validation-and-resource-limits.md) | high | **shipped** | security-grade recipe validation and resource limits | Third STAGE-006 hardening item: caps an untrusted recipe's size and step count at the single load choke point so a hostile recipe can't exhaust memory/CPU on parse or pipeline-build — the recipe analog of decode limits. |
| [SPEC-036](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-036-full-supply-chain-gate-cargo-deny-advisories-bans-sources-in-ci.md) | high | **shipped** | full supply-chain gate (cargo-deny advisories + bans + sources) in CI | Fourth STAGE-006 hardening item: turns the license-only cargo-deny CI job into the full supply-chain gate (advisories + bans + sources + licenses) so a vulnerable/unmaintained/yanked or non-crates.io dependency fails CI. |
| [SPEC-037](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-037-threat-model-verification-pass-and-final-hardening.md) | high | **shipped** | threat-model verification pass and final hardening | The STAGE-006 capstone (MVP exit gate): closes the last two concrete hardening gaps (resize output cap + save-recipe symlink guard) and records a threat-model verification pass confirming every SECURITY.md mitigation holds as built. |

### STAGE-007 — release and distribution _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-038](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-038-cargo-publish-metadata-and-dual-license-files.md) | high | **shipped** | cargo publish metadata and dual license files | First STAGE-007 step: make the crate publish-ready — complete `[package]` metadata, a lean published file set, and dual MIT/Apache license files — so a later `cargo publish` / release pipeline has a valid, professional package. |
| [SPEC-039](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-039-changelog-semver-and-release-tag-conventions.md) | high | **shipped** | changelog, semver, and release-tag conventions | Second STAGE-007 step: a seeded CHANGELOG (v0.1.0 = the MVP) + a written semver/release-tag policy, so the (later) tag-triggered release pipeline has a changelog to publish and a documented versioning contract. |
| [SPEC-040](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-040-readme-rewrite-and-shell-completions.md) | medium | **shipped** | readme rewrite and shell completions | Sixth STAGE-007 step (the last SAFE one): a user-facing README (install + usage) and clap-generated shell completions, delivering the stage's "a new user can install and run it" criterion — with no outward-facing release action. |
| [SPEC-041](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-041-release-pipeline-cargo-dist-and-msrv.md) | medium | **shipped** | release pipeline cargo-dist and msrv | Backlog #3 — the tag-triggered cargo-dist release pipeline (cross-platform binaries + checksums + installers → GitHub Releases) plus a declared MSRV, delivering STAGE-007's "reproducible release from a tag" criterion and the plumbing… |
| [SPEC-042](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-042-release-channels-homebrew-tap-and-crates-io-publish.md) | medium | **shipped** | release channels homebrew tap and crates io publish | Backlog #4 + #5 — add the Homebrew tap (cargo-dist) and a tag-triggered crates.io `cargo publish` workflow to the SPEC-041 pipeline, so one `v0.1.0` tag lights up `brew install` and `cargo install` alongside GitHub Releases. Config only —… |
| [SPEC-043](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-043-supply-chain-advisory-response-quickxml-ttfparser.md) | high | **shipped** | supply chain advisory response quickxml ttfparser | Repairs the supply-chain gate (red on `main` from ambient RustSec drift) so the release stage can proceed — adds three documented, revisit-tracked `deny.toml` advisory ignores (DEC-042) with a reachability assessment; no code change. |

### STAGE-008 — modern formats and quality _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-016](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-016-perceptual-auto-quality-shrink-to-a-visual-target.md) | high | **shipped** | perceptual auto-quality — shrink to a visual target | Delivers STAGE-008's flagship differentiator — `shrink --target visually-lossless`/`--ssim N` lets a user ask for a *look* (a perceptual quality) instead of guessing a JPEG quality number; the SSIMULACRA2 metric it adds is the foundation… |
| [SPEC-017](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-017-max-size-byte-budget-for-shrink-and-convert.md) | high | **shipped** | `--max-size <SIZE>` byte budget for `shrink` and `convert` | Delivers the other half of STAGE-008's 'tell me the outcome, not the knob' thesis — `shrink`/`convert --max-size 200KB` hits a FILE SIZE instead of a quality number, the top-ranked user pain for web image prep. Reuses the SPEC-016… |
| [SPEC-018](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-018-avif-output-behind-a-feature-gated-ravif-codec.md) | high | **shipped** | AVIF output behind a feature-gated `ravif` codec | Delivers the first MODERN output format — `convert --format avif` / `shrink -o x.avif` produce AVIF (typically 30-50% smaller than JPEG at equal quality), behind an off-by-default `avif` feature. The format-agnostic search (SPEC-017) makes… |
| [SPEC-019](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-019-webp-lossless-output-and-decode.md) | high | **shipped** | WebP lossless output + WebP decode (input), pure-Rust default | Adds WebP as a default, pure-Rust format — `.webp` becomes a readable INPUT everywhere (view/info/resize/convert), and `convert --format webp` / `-o x.webp` produce LOSSLESS WebP (smaller than PNG). All pure-Rust, zero system deps, `just… |
| [SPEC-020](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-020-lossy-webp-behind-a-feature-gated-libwebp.md) | high | **shipped** | Lossy WebP encode behind a feature-gated `libwebp` codec | Delivers the headline WebP value — LOSSY WebP (typically smaller than JPEG at equal quality) behind an off-by-default `webp-lossy` feature. Because SPEC-019 already shipped the pure-Rust WebP DECODER, BOTH auto-quality searches drive WebP… |
| [SPEC-021](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-021-max-size-dimension-reduction-fallback.md) | high | **shipped** | `--max-size` dimension-reduction fallback | Completes `--max-size`: when lowering quality alone can't hit the byte budget, downscale the dimensions until it fits. Makes `--max-size` work for LOSSLESS outputs (PNG, lossless WebP) — previously a no-op + warning — and for very small… |

### STAGE-009 — web-prep power and differentiator surface _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-022](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-022-optimize-one-button-web-good-command.md) | high | **shipped** | optimize — one-button web-good command | Delivers STAGE-009's headline one-button web-good command — the user-facing surface of the STAGE-008 perceptual + modern-format engine. |
| [SPEC-023](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-023-diff-perceptual-and-visual-comparison.md) | high | **shipped** | diff — perceptual comparison + CI gate | Delivers STAGE-009's verification surface — a perceptual SSIMULACRA2 comparison with a CI visual-regression gate, reusing the metric the auto-quality engine optimizes against. |
| [SPEC-024](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-024-responsive-picture-srcset-set-generator.md) | high | **shipped** | responsive — <picture>/srcset set generator | Delivers STAGE-009's web-delivery surface — the responsive <picture>/srcset set a web developer actually ships, generated in one command from the modern-format engine. |
| [SPEC-025](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-025-benchmark-micro-net-criterion-and-bench-recipes.md) | medium | **shipped** | benchmark micro-net (criterion) + `just bench` / `just bench-cli` | Delivers STAGE-009's credibility leg — a cheap criterion regression net for the resize/encode/decode/score/pipeline hot paths, and the equal-quality basis every future size/speed claim must rest on. |

### STAGE-010 — advisory elimination and dependency hygiene _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-044](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-044-swap-ab-glyph-for-skrifa-zeno-to-drop-ttf-parser.md) | medium | **shipped** | swap ab_glyph for skrifa+zeno to drop ttf-parser | Removes the unmaintained `ttf-parser` from the tree so STAGE-010 can delete the RUSTSEC-2026-0192 ignore toward a clean 0.2.0. |
| [SPEC-045](../projects/PROJ-001-crustyimg-mvp/specs/done/SPEC-045-in-house-tiff-ifd-exif-writer-to-drop-little-exif.md) | medium | **shipped** | in-house TIFF-IFD EXIF writer to drop little_exif | Drops `little_exif` → removes quick-xml (RUSTSEC-2026-0194/-0195) so STAGE-010 can delete two more deny ignores toward a near-clean 0.2.0. |

## <a id="proj-002"></a>PROJ-002 — the optimization engine core

*status: shipped · 4 specs, 4 shipped*

An analysis-driven decision engine: crustyimg looks at the image, decides the best format and encoding automatically — the "local f_auto" — and explains why. This turns "optimize this" from a format-and-quality guessing…

### STAGE-011 — analysis foundation _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-046](../projects/PROJ-002-optimization-engine/specs/done/SPEC-046-analysis-layer-foundation.md) | high | **shipped** | the `src/analysis/` layer — computed-once image analysis | Infrastructure enabling STAGE-011's shared Analysis layer — the computed-once context every later feature (format auto-decide, explain, lint, planner, manifest) reads. |
| [SPEC-047](../projects/PROJ-002-optimization-engine/specs/done/SPEC-047-image-classification.md) | high | **shipped** | deterministic image classification — `ImageClass` → three `OptBucket`s | Delivers STAGE-011's second half — the deterministic photo-vs-graphic verdict that biases the format engine toward the right codec family; the decisive signal every STAGE-012 auto-decision reads (`opt_bucket` + `has_alpha` → codec family). |

### STAGE-012 — auto-decide & explain _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-048](../projects/PROJ-002-optimization-engine/specs/done/SPEC-048-format-auto-decision.md) | high | **shipped** | format auto-decision in `optimize` — "the local f_auto" | Ships STAGE-012's headline: `optimize` stops preserving the input's format and starts CHOOSING the best one automatically — "the local f_auto" — the 0.3.0 differentiator. |
| [SPEC-049](../projects/PROJ-002-optimization-engine/specs/done/SPEC-049-explain-trace.md) | high | **shipped** | `--explain` — the auditable decision trace | Makes the auto-decision auditable, not a black box — the answer to the "is it just another f_auto?" risk. Prints why crustyimg picked the format, which turns the moat from "smallest file" into "smallest file, with the reasons shown." |

## <a id="proj-004"></a>PROJ-004 — image lint — "clippy for image assets"

*status: shipped · 6 specs, 6 shipped*

`crustyimg lint` — "clippy for image assets": a source-file, no-URL, deterministic, pass/fail linter you run on an `assets/`/`content/` tree in CI, BEFORE anything is deployed. It occupies the white space Lighthouse…

### STAGE-013 — lint core & shipped-capability rules _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-050](../projects/PROJ-004-image-lint/specs/done/SPEC-050-lint-command-core.md) | high | **shipped** | the `lint` command core — framework + two foundational rules | Delivers STAGE-013's foundation — the `lint` command + the Rule/Finding/Severity framework + exit-7, proven end-to-end by two high-value rules. The scaffold every other lint rule plugs into. |
| [SPEC-051](../projects/PROJ-004-image-lint/specs/done/SPEC-051-lint-config.md) | high | **shipped** | `.crustyimg-lint.toml` config + severity/select CLI | Makes `lint` tunable and quiet enough to leave on in CI — the config surface (select/ignore, per-rule severity, per-glob budgets, savings threshold) that turns a fixed rule set into a project-shaped one. |
| [SPEC-052](../projects/PROJ-004-image-lint/specs/done/SPEC-052-lint-report-json.md) | high | **shipped** | `lint --format json` report + human polish | Makes lint findings machine-readable for CI tooling — a stable, hand-rolled JSON report (no new dependency), plus the human report polish (runnable-fix line + a potential-savings summary). |
| [SPEC-053](../projects/PROJ-004-image-lint/specs/done/SPEC-053-shipped-capability-rules.md) | high | **shipped** | the shipped-capability rules | Fills out the day-one rule catalog with everything that needs only shipped capabilities — the privacy/correctness/size/colorspace rules that make `lint` genuinely useful before any engine-backed cleverness. |

### STAGE-015 — CI integration & adoption _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-056](../projects/PROJ-004-image-lint/specs/done/SPEC-056-lint-sarif-and-release-cut.md) | high | **shipped** | `lint --format sarif` + the 0.4.0 release-cut | The GitHub-native code-scanning tier (`lint --format sarif`) + the 0.4.0 release-cut that ships the whole lint wave — the notes announce "crustyimg lint + a setup-crustyimg Action + a crustyimg-action lint mode". |
| [SPEC-057](../projects/PROJ-004-image-lint/specs/done/SPEC-057-github-actions-on-ramp.md) | high | **shipped** | the GitHub Actions adoption on-ramp | The single highest-leverage distribution move: turns `crustyimg lint` into a one-line CI win — a `setup-crustyimg` installer Action + a `crustyimg-action` lint wrapper + in-repo pre-commit/recipe/docs glue — so 0.4.0 can announce "drop… |

## <a id="proj-007"></a>PROJ-007 — Reproducible build (build + cache + lockfile + `--watch`)

*status: shipped · 9 specs, 9 shipped*

Turn crustyimg from a tool that RUNS image transforms into one that BUILDS an asset tree incrementally and verifiably — "a Makefile for images." Today `apply --recipe` re-does all the work on every run and offers no…

### STAGE-020 — the `build` command + declared build manifest _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-063](../projects/PROJ-007-reproducible-build/specs/done/SPEC-063-build-command-and-manifest.md) | high | **shipped** | the `build` command + declared `crustyimg.build.toml` manifest | STAGE-020's 'declare an image build and run it with one command' capability. |

### STAGE-021 — the content-addressed cache (incremental rebuild) _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-064](../projects/PROJ-007-reproducible-build/specs/done/SPEC-064-content-addressed-cache.md) | high | **shipped** | the content-addressed cache (incremental rebuild) | STAGE-021's 'a re-run does only the work that changed' — the incremental-rebuild headline. |

### STAGE-022 — the reproducibility lockfile + `build --check` / `--frozen` _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-065](../projects/PROJ-007-reproducible-build/specs/done/SPEC-065-injective-source-output.md) | high | **shipped** | the injective source→output guarantee | STAGE-022's prerequisite — a lockfile can only pin a build whose source→output mapping is a function. |
| [SPEC-066](../projects/PROJ-007-reproducible-build/specs/done/SPEC-066-reproducibility-lockfile.md) | high | **shipped** | the reproducibility lockfile + `build --check` / `--frozen` | STAGE-022's 'a committed lockfile pins the build; --check fails CI on drift' — the 'verifiable' payoff. |

### STAGE-023 — `--watch` — the debounced rebuild inner loop _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-067](../projects/PROJ-007-reproducible-build/specs/done/SPEC-067-build-watch.md) | medium | **shipped** | `crustyimg build --watch` | STAGE-023's 'a debounced rebuild inner loop' — the dev ergonomics leg of the build. |

### STAGE-024 — hardening & security sweep _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-068](../projects/PROJ-007-reproducible-build/specs/done/SPEC-068-proj007-threat-model.md) | high | **shipped** | PROJ-007 threat-model / attack-surface review | STAGE-024's LEAD — the systematic threat-model pass over PROJ-007's new untrusted-input surface; its findings reprioritize the rest of the stage. |
| [SPEC-069](../projects/PROJ-007-reproducible-build/specs/done/SPEC-069-decoder-fuzz-gate.md) | high | **shipped** | run the decoder fuzz gate (AVIF / SVG / RAW / HEIC) | STAGE-024's #1 (High) — the pre-1.0 decoder fuzz gate the roadmap requires but that was never run; the one untrusted-binary surface this wave's review could not close by reading. |
| [SPEC-070](../projects/PROJ-007-reproducible-build/specs/done/SPEC-070-bound-peak-decode-memory.md) | high | **shipped** | bound peak decode memory | STAGE-024's peak-memory hardening — closes the F-RAW-1 / general-decode memory-amplification residual SPEC-069 surfaced; the untrusted-input-hardening posture's missing memory bound. |
| [SPEC-071](../projects/PROJ-007-reproducible-build/specs/done/SPEC-071-hardening-cleanup-batch.md) | medium | **shipped** | STAGE-024 hardening cleanup (batch) | STAGE-024's small-fix tail, batched — the point-fixes the LEAD review + the fuzz/peak-memory specs surfaced, done in one proportionate cycle rather than four spec lifecycles. |

## <a id="proj-008"></a>PROJ-008 — WASM core + demo page

*status: shipped · 33 specs, 33 shipped*

Compile crustyimg's already-I/O-agnostic pure-Rust core to WebAssembly and ship two artifacts over it: an **npm-packaged library** that runs the engine (decode → transform/optimize → encode) entirely **client-side, with…

### STAGE-025 — WASM core build (the load-bearing probe) _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-072](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-072-wasm-build-seam-and-baseline.md) | high | **shipped** | WASM build seam + baseline (AVIF-decode gated out) | The load-bearing baseline of STAGE-025: proves the pure engine runs a real decode → transform → encode round-trip in wasm with no backend, establishing the build seam every other PROJ-008 stage layers over. |
| [SPEC-073](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-073-avif-on-wasm-decision.md) | high | **shipped** | AVIF-on-wasm decision (encode in, decode deferred) + DEC | Resolves the AVIF-on-wasm question STAGE-025 was organized around: proves and wires the "convert to AVIF in-browser" headline (rav1e encode), and records the decode asymmetry as a DEC — so the demo's compelling moment is real and honestly… |
| [SPEC-074](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-074-wasm-bundle-size.md) | medium | **shipped** | WASM bundle size | Shrinks the demo bundle so "zero-install, instant try it" is actually instant — the wave's main technical debt (1.52 MB brotli). Completes STAGE-025. |

### STAGE-026 — npm-packaged library _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-075](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-075-npm-package-shape-and-smoke-test.md) | high | **shipped** | npm package shape + identity + install smoke test | Makes STAGE-025's proven WASM core an installable, identified npm package that runs client-side with no native addon — the "sharp without the native addon" artifact, and the package STAGE-027's demo consumes. |
| [SPEC-076](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-076-publish-crustyimg-wasm-to-npm-gated.md) | high | **shipped** | publish crustyimg-wasm to npm (gated) | Doubles the launch story: crustyimg as a zero-dependency JS/TS library (crustyimg-wasm) alongside the CLI — no native addon, runs in the browser/Node. The outward-facing npm publish, gated. |

### STAGE-027 — demo page _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-077](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-077-demo-skeleton.md) | high | **shipped** | demo skeleton (in-browser, single-threaded) | The first end-to-end proof that crustyimg runs as a real in-browser web page — drop an image, get an optimized result, download it — the skeleton STAGE-027's "watch it just work" demo is built on. |
| [SPEC-078](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-078-demo-worker-avif-explain.md) | high | **shipped** | demo — Web Worker, AVIF, and the explain readout | Completes STAGE-027: makes the demo's headline (drop a PNG → tiny AVIF) real without freezing the tab, reads `.avif` inputs, and shows the decision (bytes saved / format / why) — the full "watch it just work" experience. Its ship completes… |

### STAGE-028 — launch readiness _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-082](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-082-readme-front-door.md) | high | **shipped** | README front door | The front door for the r/rust-first launch — and the page crates.io renders. Turns a good CLI reference into a launch README: the browser demo, the why-vs-alternatives, honest install. |
| [SPEC-083](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-083-honest-benchmarks.md) | high | **shipped** | honest benchmarks (BENCHMARKS.md) | The credibility spine of the launch — honest, equal-quality, reproducible numbers vs sharp/squoosh/ ImageMagick. The claims r/rust and HN will actually scrutinize. |
| [SPEC-100](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-100-readme-ci-actions-section-surface-raw-and-recipes.md) | high | **shipped** | README CI/Actions section + surface RAW and recipes | Surfaces two under-marketed differentiators in the launch README — the CI GitHub Actions and the RAW/recipes story — before the 0.5.0 cut. Small, ships with the README. |
| [SPEC-102](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-102-avif-in-the-distributed-binary.md) | high | **shipped** | AVIF in the distributed binary | Closes the gap between what BENCHMARKS.md measures and what a `brew install` user gets — the flagship AVIF path is currently absent from every distributed binary. |

### STAGE-029 — demo launch quality _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-079](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-079-wasm-optimize-surface-speed-budget-score-auto-avif.md) | high | **shipped** | wasm optimize surface — speed, budget, score, auto-avif | Provides the engine surface STAGE-029's demo redesign (SPEC-080) and SSIMULACRA2 readout (SPEC-081) consume: a per-call encode speed, a size/quality target, a returned perceptual score, and an Auto path that actually shrinks a photo… |
| [SPEC-080](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-080-demo-intent-redesign-and-perf-ux.md) | high | **shipped** | demo = the `web` flow, one-click, with a CLI adoption funnel | Make the demo BE the flagship. The live demo is a squoosh-style "pick a format" tool that defaults to lossless-WebP (which makes a photo BIGGER). Reframe it to the shipped `web` flow as one opinionated action — drop a photo, it downscales… |
| [SPEC-081](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-081-demo-ssimulacra2-diff-ui.md) | high | **shipped** | demo SSIMULACRA2 diff UI | The demo's differentiator vs squoosh: we don't guess the quality, we measure it. Shows the input↔output SSIMULACRA2 score so "smaller AND still looks right" is proven on the page, not asserted. |
| [SPEC-095](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-095-wasm-avif-quality-align-to-web.md) | medium | **shipped** | align the wasm default AVIF quality to native `web` (q80 → q85) | Make the demo a FAITHFUL preview of `crustyimg web`, not an over-flattering one. The wasm AVIF path encodes at q80 while native `web` uses q85 — so the demo shows a SMALLER file than the CLI actually produces, overstating the savings a… |
| [SPEC-096](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-096-demo-pre-launch-polish-plain-recipe-copy-crab-spinner.md) | medium | **shipped** | demo pre-launch polish — plain recipe copy + crab spinner | Pre-launch polish so the demo and the recipes a visitor copies read like a human wrote them, and the busy state doesn't imply "stuck". Cheap credibility before Show HN. |
| [SPEC-101](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-101-demo-polish-v2-ssimulacra2-explainer-links-ff-safari-band-check.md) | medium | **shipped** | demo polish v2 — SSIMULACRA2 explainer links + FF/Safari band check | Small pre-launch demo polish: make the SSIMULACRA2 score self-explaining (link the metric) and confirm the score band renders correctly on Firefox/Safari before real users hit it. |
| [SPEC-103](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-103-wire-raw-decode-into-the-demo-behind-a-pixel-gate.md) | medium | **shipped** | wire RAW decode into the demo behind a pixel gate | Closes the demo's RAW gap — the front-door pitch says "sharp and squoosh can't open these," so a photographer dropping a .dng must get a preview, not a leaked "Tiff is not supported" error. |
| [SPEC-104](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-104-raise-the-demo-raw-preview-gate-40-to-60-megapixels.md) | medium | **shipped** | raise the demo RAW preview gate 40 → 60 megapixels | On-device tuning of SPEC-103's RAW gate: the 40 MP default wrongly blocked a real Leica DNG on desktop, where memory is ample — raise the gate so real RAW files convert in the demo. |
| [SPEC-105](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-105-high-entropy-images-are-never-graphics-fix-grayscale-photo-misclassification.md) | high | **shipped** | high-entropy images are never graphics — fix grayscale photo misclassification | The demo's headline "drop a photo → watch it just work" mis-encodes a real grayscale photograph as a 13× oversized lossless file. Fix the shared content classifier so a high-entropy image is never treated as a graphic. |

### STAGE-030 — command taxonomy & CLI-quality freeze _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-084](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-084-fixed-quality-avif-default-two-regime-quality-never-bigger.md) | high | **shipped** | fixed-quality AVIF in the default path + two-regime quality + never-bigger | The engine foundation for STAGE-030: make the DEFAULT format decision fast and AVIF-aware so the verb everyone runs stops optimizing the least-valuable variable. Measured: today's default gets 24% median in 16.5 s (2/8 passthrough, 49 s on… |
| [SPEC-085](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-085-web-flagship-verb-and-bundled-recipe-registry.md) | high | **shipped** | `web` flagship verb + bundled-recipe registry | Ships the measured flagship: the `web` flow (downscale → content-modernize to AVIF/lossless-WebP → never-bigger → strip → orient → prove the score) that got 98% median savings in 2.7 s, size-insensitive, vs today's default 24% in 16.5 s.… |
| [SPEC-086](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-086-redefine-optimize-keep-dims-add-verify-remove-shrink.md) | high | **shipped** | redefine `optimize` (keep-dims + `--verify`) + remove `shrink` | Finishes the two-tier story: `optimize` is the honest keep-dimensions byte-primitive (fast, never bigger, opt-in proof via --verify), and the redundant `shrink` verb — whose downscale-re-encode `web` now does better with AVIF — is removed.… |
| [SPEC-087](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-087-meta-group-consolidation-strip-clean-copy.md) | medium | **shipped** | `meta` group consolidation (strip / clean / copy) | One intent per verb-group: fold the scattered metadata verbs (strip / clean / copy-metadata) into a single `meta` group with subcommands, so the top-level surface reads as distinct *jobs* (web, optimize, convert, resize, meta, …) instead… |
| [SPEC-088](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-088-unified-audit-report-json-timing-and-committed-bench.md) | high | **shipped** | unified audit report (`--json`/`--timing`) + committed bench | Makes crustyimg's results measurable and reproducible: a consistent machine-readable report (`--json`) + `--timing` across the audit-relevant verbs, and a COMMITTED benchmark harness + a small license-clean corpus. This is what… |
| [SPEC-089](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-089-fold-set-into-meta-set.md) | medium | **shipped** | fold `set` into `meta set` | Completes the `meta` group: fold the existing top-level `set` verb (write EXIF attribution tags) into `meta set`, so ALL four metadata operations — remove-all / remove-GPS / copy / write — read as one job under `meta`, instead of `set`… |
| [SPEC-090](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-090-web-never-bigger-baseline-reconciliation.md) | high | **shipped** | reconcile `web`'s never-bigger claim with its actual baseline | The flagship verb's headline promise must be true as a user reads it. `web` claims "never bigger", but enforces that against the DOWNSCALED intermediate, not the file the user handed in — so an already-small >2048px source can come back… |
| [SPEC-091](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-091-avif-decode-thread-policy.md) | high | **shipped** | AVIF decode thread policy (cap dav1d's threads; kill the DisjointMut flake) | Set an explicit AVIF-decode thread policy. Today every decoder instance spawns dav1d's auto thread pool (all cores) to decode a SINGLE still frame — inside a rayon batch that already parallelizes across files. That oversubscription is very… |
| [SPEC-093](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-093-metadata-write-corrupts-numeric-tags.md) | high | **shipped** | the metadata write path corrupts numeric EXIF tags | The metadata container lane silently CORRUPTS numeric EXIF tags. `meta clean --gps` — the privacy verb, documented to preserve "orientation, copyright, ICC" — rewrites Orientation 6 → 1536, so the user's photo displays rotated wrong in… |
| [SPEC-094](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-094-guard-empty-obu-avif-decode.md) | medium | **shipped** | guard the empty-OBU `debug_abort()` in AVIF decode | Close an uncatchable-abort hole in AVIF decode. An EMPTY OBU stream reaches re_rav1d's debug-only `debug_abort()`, which bypasses both our `catch_unwind` and SPEC-091's scoped-thread `join` (abort ≠ unwind) — so in a DEBUG build (where the… |

### STAGE-031 — engineering quality and code health _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-097](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-097-decompose-cli-mod-rs-into-a-submodule-tree-and-dedup-escape-json.md) | medium | **shipped** | decompose cli/mod.rs into a submodule tree and dedup escape_json | Makes the CLI legible and safe to change post-launch — one 6.5k-line file becomes a submodule tree and a duplicated JSON escaper becomes one source of truth — with byte-identical behavior. |
| [SPEC-098](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-098-dependency-pinning-strategy-decision-record.md) | medium | **shipped** | dependency-pinning strategy decision record | Closes the audit's D4 thread with a documented pinning policy, removing an open question and pre-recording the exact prerequisite the future crates.io publish must satisfy. |
| [SPEC-099](../projects/PROJ-008-wasm-core-and-demo/specs/done/SPEC-099-crates-io-pinning-correction-caret-migrate-library-public-deps-supersede-dec-078.md) | high | **shipped** | crates.io pinning correction — caret-migrate library-public deps + supersede DEC-078 | Corrects a false premise in a just-shipped decision and stops us publishing a consumer-hostile library: crustyimg IS on crates.io, so the exact `=` pins bite `cargo add crustyimg` NOW. |

## <a id="proj-009"></a>PROJ-009 — Input reach (modern-format decode)

*status: shipped · 4 specs, 4 shipped*

Broaden what crustyimg can READ so the already-shipped engine (optimize / convert / lint / info / responsive) applies to the modern-format assets developers actually have — AVIF, SVG, camera RAW — from the pure-Rust,…

### STAGE-016 — AVIF decode as a default, pure-Rust input _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-058](../projects/PROJ-009-input-reach/specs/done/SPEC-058-avif-decode-input.md) | high | **shipped** | AVIF decode as a default, pure-Rust input | STAGE-016's 'read AVIF from the default pure-Rust build' capability. |

### STAGE-017 — SVG rasterize as a default, pure-Rust input _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-060](../projects/PROJ-009-input-reach/specs/done/SPEC-060-svg-rasterize-input.md) | high | **shipped** | SVG rasterize as a default, pure-Rust input | STAGE-017's 'rasterize `.svg` to the canonical raster Image from the default pure-Rust build' capability. |

### STAGE-018 — RAW Tier-1 embedded-preview extraction as a default input _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-061](../projects/PROJ-009-input-reach/specs/done/SPEC-061-raw-embedded-preview-input.md) | high | **shipped** | RAW Tier-1 embedded-preview extraction as a default input | STAGE-018's 'extract the embedded full-res JPEG preview from common RAW on the default build' capability. |

### STAGE-019 — HEIC decode behind an off-by-default `heic` feature _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-062](../projects/PROJ-009-input-reach/specs/done/SPEC-062-heic-decode-feature-gated.md) | high | **shipped** | HEIC decode behind an off-by-default `heic` feature | STAGE-019's 'read HEIC only under --features heic; default binary exits 4' capability. |

## <a id="proj-010"></a>PROJ-010 — Post-launch correctness and consolidation

*status: active · 17 specs, 13 shipped*

Fix the launch-gating classifier regression (dithered/halftoned graphics promoted to lossy AVIF after resize), confirm hostile-input behavior on the native CLI and wasm builds, then deliver three carried-forward stages…

### STAGE-034 — Classifier regression fix _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-108](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-108-classification-placement-and-scale-aware-entropy.md) | critical | **shipped** | classification placement and scale-aware entropy | STAGE-034's whole reason to exist: stop `web` promoting dithered and halftoned graphics to lossy AVIF because the resize changed their entropy. |
| [SPEC-109](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-109-evidence-integrity-for-the-classifier-calibration.md) | critical | **shipped** | evidence integrity for the classifier calibration | Builds the instrument SPEC-108 is measured with. Until a threshold mutation can make a test go red, nothing in STAGE-034 is proven. |

### STAGE-035 — Hostile/edge input confirmation pass _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-107](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-107-hostile-and-edge-input-confirmation-pass.md) | critical | **shipped** | hostile / edge input confirmation pass | STAGE-035's whole reason to exist: move the launch-readiness hostile-input item from "hold natively; confirm in the browser" to a driven, recorded outcome — and fix what driving it found. |

### STAGE-039 — shipped-verb correctness _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-110](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-110-orientation-baked-on-every-pixel-lane-verb.md) | critical | **shipped** | bake EXIF Orientation on every pixel-lane verb | STAGE-039's D-1: stop the shipped verbs handing back a sideways image, and make one orientation rule true across the whole pixel lane instead of six verbs being wrong by accident. |
| [SPEC-111](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-111-build-runs-bundled-recipes.md) | critical | **shipped** | `build` runs bundled recipes | STAGE-039's D-2: `build` can actually run the recipes it ships with, and a recipe saved by `edit` reproduces what `edit` did. |

### STAGE-040 — release readiness for 0.7.0 _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-112](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-112-wasm-transform-runs-bundled-recipes.md) | critical | **shipped** | `wasm::transform` runs the bundled recipes | STAGE-040's precondition for the 0.7.0 cut: make the README's claim about `transform()` true before that README renders on the crates.io crate page. |

### STAGE-042 — release-safety instruments _(active)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-118](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/SPEC-118-shipped-surface-conformance-matrix.md) | high | design | the shipped-surface conformance matrix | STAGE-042's central instrument. Every PROJ-010 defect so far has been an UNENUMERATED CELL — a verb × input × entry-point combination nobody listed, so nobody tested. This spec builds the enumeration itself, and it would have caught… |
| [SPEC-123](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/SPEC-123-avif-byte-determinism-across-thread-counts.md) | high | design | is AVIF output byte-deterministic across thread counts? | The repo's reproducible-build story — `build --frozen`, the lockfile, the cache key — rests on byte-stable output. Upstream gives no such guarantee for AVIF and has a filed nondeterminism bug. Nobody has measured whether the claim we… |

### STAGE-043 — pinned-path correctness _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-113](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-113-optimize-never-silently-grows-a-pinned-output.md) | critical | **shipped** | `optimize` never silently grows a pinned same-format output | STAGE-043's first item. `optimize` is a headline verb the launch post will name, and on its most natural invocation it can silently hand back a file twice the size of the source. Fixing it before anyone is invited to try the tool is the… |
| [SPEC-116](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-116-build-threads-the-truncated-jpeg-warning.md) | medium | **shipped** | `build` threads the truncated-JPEG warning | STAGE-043's second and last item. `apply --recipe web bad.jpg` warns that a truncated JPEG decoded partially; `build` on the identical input is silent. A user who moved from `apply` to `build` silently lost a warning the project already… |

### STAGE-044 — the `meta` lane cannot emit a broken manifest _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-114](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-114-meta-lane-never-emits-a-broken-manifest.md) | critical | **shipped** | the `meta` lane never emits a broken manifest | STAGE-044. `meta set` takes a file whose Content Credentials validate and emits one a validator reports as TAMPERED. Attributing a forgery to the file's signer is a worse failure than dropping the credentials, and it is live in 0.7.0. |

### STAGE-045 — adopted-source-format integrity on the decide path _(shipped)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-115](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-115-optimize-never-passes-through-bytes-it-cannot-name.md) | high | **shipped** | `optimize` never passes through bytes it cannot name | STAGE-045's only spec. `optimize` on the three input families PROJ-009 delivered can ship the source container verbatim — vector XML, a HEIF container, a whole camera-raw file — labeled as a PNG or JPEG it never produced. On stdin it… |
| [SPEC-117](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-117-pin-build-and-apply-against-the-adopted-format-defect.md) | medium | **shipped** | pin `build` and `apply --recipe web` against the adopted-format defect | STAGE-045's last item. SPEC-115 fixed the adopted-format defect at one seam that four verbs share. Its verify DROVE `build` and `apply --recipe web` green on the real binary but pinned neither, so a refactor can re-break them in silence… |

### STAGE-046 — Output Fidelity on Shipped Verbs _(proposed)_

| Spec | Priority | Status | Title | Summary |
|---|---|---|---|---|
| [SPEC-119](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-119-animated-input-is-never-silently-flattened.md) | critical | **shipped** | animated input is never silently flattened | STAGE-046's first and most urgent item. The tool accepts a valid animated file, discards every frame but the first, reports the loss as a 72% win with a perfect quality score, and — through `lint` — actively recommends the command that… |
| [SPEC-120](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/done/SPEC-120-measure-the-linear-light-premise.md) | high | **shipped** | measure the linear-light premise before fixing it | STAGE-046's falsification gate. The linear-light entry set its own premise test and this spec runs it, so the repo either fixes a measured defect or closes an unmeasured one — instead of shipping a plausible improvement. |
| [SPEC-121](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/SPEC-121-ops-preserve-colour-type-and-bit-depth.md) | high | design | ops preserve colour type and bit depth | STAGE-046's largest correctness item. Three op bodies widen every image to RGBA8 and never narrow back, so `resize`, `thumbnail`, `edit` and the flagship `web` add an all-opaque alpha channel (+12.4% bytes, measured) and halve 16-bit input… |
| [SPEC-122](../projects/PROJ-010-post-launch-correctness-and-consolidation/specs/SPEC-122-resize-resamples-in-linear-light.md) | high | design | `resize` resamples in linear light | STAGE-046's last defect, and the only one whose premise was measured before it was specced. `Resize::apply` resamples non-linear sRGB as if it were linear; against an independent reference the shipped downscale scores 70.45 and 84.45 where… |
