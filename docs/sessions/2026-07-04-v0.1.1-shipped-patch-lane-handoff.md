# Handoff — v0.1.0 + v0.1.1 shipped; patch lane established; next = 0.2.0 advisory elimination

Paste the block below into a fresh **Opus** session in the `crustyimg` repo. This session
took the MVP **public** — shipped **v0.1.0** and **v0.1.1** to crates.io + the Homebrew tap
+ GitHub Releases, established and validated the **patch lane** (DEC-043, PATCH-001), added
a `just release` version guard, and produced a framework-feedback harvest for the template
author. Process mechanics are unchanged (also in auto-memory).

---

You are the ORCHESTRATOR / architect for crustyimg — a pure-Rust, permissive
(MIT OR Apache-2.0) image CLI built spec-driven. Repo root:
/Users/jyashinsky/PSeven/experiments/crustimg_redo_plus/crustyimg (own git repo,
remote git@github.com:jysf/crustyimg.git, gh authed). main is clean at c71d848,
all CI green. Pause for me before every merge and before any outward-facing action.

## Orient (read first)
- AGENTS.md (§4 cost, §5/§6 gates, §13 git/PR, §15 build cycle).
- docs/backlog.md — the "Post-0.1.0 fast-follows" section is your next work.
- docs/framework-feedback/{process-feedback,signals-harvest}.md — the framework retro.
- decisions/DEC-042 (accepted advisories), DEC-043 (the patch lane), DEC-040/041
  (release pipeline), RELEASING.md (+ the `just release` shortcut).
- Run `just status` / `just specs-by-stage`.

## Where we are (2026-07-04) — the MVP is SHIPPED and PUBLIC
- PROJ-001 (crustyimg MVP) is done and installable: **v0.1.0 + v0.1.1** live on
  crates.io, the Homebrew tap (jysf/homebrew-tap), and GitHub Releases.
  `cargo install crustyimg` / `brew install jysf/tap/crustyimg` both work.
- STAGE-007 (release & distribution) complete: #1–#6 shipped; #7 (dual lean/full
  artifacts) deferred. The tag-triggered cargo-dist pipeline is proven across 2 releases;
  `just release X.Y.Z` guards tag==Cargo.toml version + CHANGELOG before the tag push.
- **Patch lane established (DEC-043) and validated** by PATCH-001 (--out-dir auto-create,
  shipped in v0.1.1): a bounded fix to shipped behavior runs patch→verify→ship (keeps
  independent verify + DECs + gates; sheds frame/design + stage bookkeeping). Lives in
  projects/PROJ-001-crustyimg-mvp/patches/. See docs/development.md.
- Docs reorganized: README is tool-first (install/usage), docs/cli-reference.md is the full
  CLI reference, docs/development.md is the contributor/spec-driven-workflow guide.
- crustyimg ships with 3 accepted deny.toml advisory ignores (DEC-042): quick-xml
  RUSTSEC-2026-0194/0195 (via little_exif) + ttf-parser RUSTSEC-2026-0192 (via ab_glyph) —
  assessed not-reachable + STAGE-006-bounded, no upstream fix. These are the 0.2.0 targets.

## Your next task: the 0.2.0 "advisory elimination" arc (docs/backlog.md)
Each removes a deny.toml ignore → a clean 0.2.0. Recommended order:
1. **ab_glyph → fontdue** — drop unmaintained ttf-parser. fontdue 0.9.3 (MIT/Apache/Zlib,
   pure-Rust, its own parser). Swap the watermark-text rasterizer; verify output parity.
   (Likely a small SPEC — a dep swap with behavior tests — or a PATCH if it stays tight.)
2. **In-house EXIF-tag writer** — drop little_exif (+ its quick-xml vulns). A binary
   TIFF-IFD writer for `set`/`clean --gps` via img-parts (already a dep); no XMP → no
   quick-xml. Meatier (M) → a SPEC. (kamadak-exif/nom-exif are read-only; no drop-in.)
3. **--help jargon cleanup** — remove STAGE-00X/DEC-0XX refs + stale "stub" text from the
   clap doc-comments in src/cli/mod.rs. A PATCH.
Then cut 0.2.0 via `just release 0.2.0` → pause for me before the tag push.

Also open (not urgent): STAGE-007 #7 (dual lean/full artifacts); reconcile
docs/development.md ↔ docs/USAGE.md overlap; a scheduled cargo-deny advisory CI job
(retro rec); future PROJ-002 (crop lead, effects, RAW-input via embedded-preview, HEIC
via pre-convert — all in docs/backlog.md + guidance/license-watchlist.yaml).

## Process mechanics (do NOT relearn — also in auto-memory)
- Spec: design (Opus, you) → build (Sonnet, prescriptive prompt, model: "sonnet") →
  verify (independent Explore subagent, Opus) → ship. Push design+DECs to main FIRST.
- Patch (DEC-043): patch (Sonnet, design+build in one pass) → independent verify → ship.
- Run the lean build (cargo build --no-default-features) AND full `cargo deny check
  advisories bans sources licenses` in build + verify.
- VERIFY THE GIT INDEX before every ship commit (git show :file; editor/linter re-stages
  stale content). Confirm cost-audit + lean + supply-chain green on main after push.
- Build/verify subagents can die on API overload mid-cycle and background subagents can't
  get Bash — dispatch foreground; if one dies, verify its partial output and finish in the
  main loop (this happened twice and worked).
- Outward-facing (git tag / cargo publish / gh release / tap) = MAINTAINER-AUTHORIZED:
  never without my explicit go in the moment. Use `just release X.Y.Z` to prep + guard.
- Framework-feedback lives in docs/framework-feedback/; two upstream prompts (patch lane +
  release guard) were already handed to the spec-driven-template author.

Start by confirming state (just status), then design/scope fast-follow #1 (ab_glyph →
fontdue). Pause before every merge and before any outward-facing release action.
