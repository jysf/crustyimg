# SPEC-088 timeline

Architect appends as cycles are designed. Executors update status as
they go. Status markers: `[ ]` not started · `[~]` in progress · `[x]` complete · `[?]` blocked.

Cycle prompts live in `prompts/SPEC-088-<cycle>.md`.

## Instructions

- [x] **design** — spec + failing tests + implementation context written to `main`.
- [x] **build** — audit report (`--json`/`--timing`) + committed bench; worktree `spec-088-audit-bench`, PR #92, ~$4.90 est, DEC-074. Gates green (731 default / 744 avif). 2026-07-16.
- [x] **verify** — independent adversarial pass, own worktree. ⚠ **PUNCH LIST** (4 items; byte-identity + privacy PROVEN clean against the pre-spec oracle). ~$3.40 est. 2026-07-16.
- [x] **fix** — punch list cleared (4/4) + the maintainer's corpus ruling (both halves): `--json`+`-o -` guarded at the shared writer (incl. the pre-existing `--explain=json` — DEC-074 §Corrections); real `docs/cli-reference.md` §"Audit surface" + DEC-074 #3 give the lint criterion actual evidence; DEC-028 de-staled; a real CC0 photo (`photo_forest_cc0.jpg`, classifier-verified `photograph`) makes the AVIF claim true — it is the only row reaching AVIF; `photo_*.jpg` → `gradient_*.jpg` (engine says `graphic-logo`); `print_table` caveat footer. Byte-identity re-proven vs the oracle (32/32); gates green (732 default / 745 avif). ~$4.05 est. 2026-07-16.
- [x] **re-verify** — independent pass over the fix commit (9add66b), own worktree. ⚠ **PUNCH LIST (2 minor, doc-accuracy only)**. **Licence + provenance: CONFIRMED, high confidence** — every element re-read from the Commons API (`Artist`=DimiTalen, `Credit`=Own work, `License`=cc0, `AttributionRequired`=false) and the committed file proven to BE that photograph by pixel diff vs the Commons original (ssim 53 vs −816 unrelated / 100 self). Bare JFIF confirmed by a raw segment walk (source GPS 50.4957/6.1034 absent). AVIF genuinely exercised (winner avif, 30%, ssim 81.4; confirmed by `file` + macOS `sips`). `-o -` guard fires exit 2 / 0 stdout bytes on every verb+spelling, and its test provably fails when the guard is removed. DEC-074 §Corrections reproduced EXACTLY (15,963 B, magic at offset 514). Byte-identity independently swept **32/32**. All docs values driven true; footer prints/suppresses; generator byte-for-byte. Gates green (732/745). **Defects:** (1) spec ~L385 cites `flat_ratio 0.02 / entropy 7.58` — those are the **960px intermediate's**; the committed file measures **0.04 / 7.37** (conclusion + thresholds still correct); (2) `Cargo.toml:303` still says criterion "runs via `just bench`" → `just bench-micro`. ~$3.30 est. 2026-07-16.
- [x] **orchestrator spot-check of fix round 1** — ⚠ **FOUND A THIRD DEFECT BOTH VERIFY PASSES MISSED.** The round-1 guard keyed on the `-o -` **spelling**, not the **state**, so the DEFAULT stdout sink walked through: `web photo.jpg --json` → **exit 0, 126,068 B** of JSON-then-image, unparseable — the exact corruption DEC-074 §Corrections condemns, via the other door, on the flagship verb + the flag SPEC-088 added. Driven, with the working path (`-o FILE --json` → 504 B clean JSON) and the pre-spec oracle (identical, 126,052 B) as controls. Chartered fix round 2. **Lesson: the guard was tested where it was IMPLEMENTED, not where the criterion APPLIES.** 2026-07-16.
- [x] **fix round 2** — all three cleared. Guard re-keyed on the STATE (`image_sink_is_stdout()`: no `--out-dir` && output is none-or-`-`) at the shared writer → one rule, every verb × both spellings × both doors; test 4 → 8 cases + a positive control, proven real by **two** mutations (guard removed → fails; guard reverted to the round-1 `-o -` condition → **fails on a door-2 case**, proving the new coverage does work). Photo evidence corrected to the SHIPPED artifact's driven values (`flat_ratio 0.04 / entropy 7.37`). `Cargo.toml:303` → `bench-micro`. Byte-identity re-proven vs the 913faef oracle (**55 runs**: 25 file-sink + 30 stdout-sink, 0 diffs). Reported-not-fixed (out of scope): a PRE-EXISTING `--features avif` flake in re_rav1d's debug-only DisjointMut overlap check. ~$3.15 est. 2026-07-16.
- [x] **orchestrator spot-check of fix round 2** — ✅ **CLEAN.** Guard closed AND correctly scoped: 4 verb×door combos all exit 2 / 0 stdout bytes, while `-o FILE --json` (504 B clean JSON, 30%), `--out-dir DIR --json` (504 B clean), and `--timing` (image → stdout, timing → stderr) all still work. Both doc fixes landed; remaining `7.58` hits are narrative describing the correction. README class column driven against the engine: `gradient_small.jpg` → `graphic-logo` 0%, `photo_forest_cc0.jpg` → `photograph` / avif / 30% — both match. 2026-07-16.
- [x] **ship** — `gh pr update-branch` (PR was BEHIND) → CI matrix polled green → squash-merged PR #92 (**a6b74de**) 2026-07-16; main synced, worktrees + branch removed. Bookkeeping: cycle→ship, 7 cost sessions (~$19.75 total), timeline, STAGE-030 → 5/6, `just archive-spec`, memory + brag. Three lessons banked. Two follow-ups filed: the classifier-drift regression test, and the re_rav1d DisjointMut investigation. ~$0.95 est.
