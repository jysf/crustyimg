# SPEC-081 timeline

Architect appends as cycles are designed. Executors update status as
they go. Status markers: `[ ]` not started · `[~]` in progress · `[x]` complete · `[?]` blocked.

Cycle prompts live in `prompts/SPEC-081-<cycle>.md`.

## Instructions
- [x] design — framed 2026-07-13; **RECONCILED against the shipped SPEC-080/095 demo 2026-07-18**: extend the shipped `renderScore` + `decodeInBrowser` seams (import `score` into the worker), score the hero AVIF via browser-decode + `score()`, and present the RAW SSIMULACRA2 value on an honest band (not clamped to 0–100 — can exceed 100 / go negative). Build after SPEC-095 (shipped). Complexity S.
- [x] build — Opus, primary checkout (demo files only, no `src/`/wasm change). Score attached honestly per output: `engine` (JPEG), `browser` (AVIF — the SPEC-080 gap, closed via `readBack`'s existing decode + `score()`), `lossless`, `unavailable`. Banded panel handles negatives/>100. Deviation: AVIF `score()` reference is the *downscaled* PNG (dims must match), consistent with the engine's own JPEG scoring; added a sixth "very low" band. Smoke green; observed AVIF **80.2/"high"**, JPEG **91.0**, 0 network requests.
- [x] verify — ✅ CLEAN (Opus, primary checkout, adversarial). Scope invariant: `git diff --stat main...` = demo/spec/smoke only, 0 engine bytes. Every honesty claim gated by a negative control: NC-1 masquerade (`score(px,px)=100.0` vs real `80.17` → browser path is a real diff, no fallback); NC-2 negative (`-4.7` renders raw on "very low", unclamped); NC-3 `>100` (`112.5` unclamped); NC-4 break-the-gate (breaking `score()` fails `avif_shows_browser_score`). Numbers reproduced from a fresh from-source wasm build. Deviation #1 wording checked — sits under the "Resized to…" note, no "vs your original" overclaim. Verify-only smoke hardening (`6f1b9c1`) now asserts the meter fill. Caveat: Chrome-only smoke; `color-mix()` degrades gracefully → FF/Safari glance before launch.
- [x] ship — squash-merged PR #100 (**60511f5**) 2026-07-18, full 27/0 matrix green. The demo's hero AVIF output is now scored in-page (browser-decode + SSIMULACRA2 on an honest band) = the squoosh differentiator, live. No DEC. Bookkeeping: cycle→ship, 3 cost sessions with `model:` (build Opus $3.0 / verify Opus $3.5 / ship $0.45 ≈ **$6.95**), timeline, archive, memory + brag. **SPEC-081 = the last STAGE-029 demo piece.** Process note: orchestrator mis-started the build (spawned subagent → over-corrected to manual handoff) before landing the hand-off-a-prompt-and-stay-out pattern; held clean through verify+merge. Two launch-readiness carries → STAGE-028: FF/Safari `color-mix()` glance; q85≈"high" quality-story note for BENCHMARKS.
