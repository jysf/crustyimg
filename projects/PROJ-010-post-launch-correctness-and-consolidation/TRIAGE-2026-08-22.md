# PROJ-010 backlog triage — 53 open items across 8 non-shipped stages

**Run 2026-08-22.** Every open `- [ ]` in PROJ-010 read and assessed as a set for the first
time — they accreted over a wave and had never been reviewed against each other. Method
matches the one used on the two external review batches: verify the item still applies,
then keep / move / defer / drop, with a reason.

**The headline: this is a project with a tail, not a graveyard.** ~24 of 53 are real and
actionable. But **6 belong to PROJ-011**, **1 was already fixed**, and **~15 are things
nobody is going to do** — and while those sit as `- [ ]` they hide the ones that matter.

| verdict | n | meaning |
|---|---:|---|
| **already done** | 1 | fixed and never marked |
| **move to PROJ-011** | 6 | its thesis, not this one's |
| **keep — actionable** | ~24 | real, with a named defect or a measured cost |
| **defer / low** | ~15 | no demand signal, or framework polish |
| **maintainer status needed** | 4 | STAGE-041 — cannot be triaged from inside the repo |
| **duplicated across projects** | 1 | claimed by two documents |

---

## Already done — corrected in this pass

- **STAGE-042 · the `pages` Chrome-startup race** — fixed by **PR #186 → `dcd43c8`**, marked
  `[x]`, count re-derived to `14 [ ] + 6 [x] = 20`. It was still filed as *"red on `main`
  right now"* after being fixed the same day.

## Move to PROJ-011 — its thesis, not this one's

These six are the work PROJ-011's brief already claims. **Leaving them here means two
documents plan the same thing**, which is how a stage gets built twice or not at all.

| item | from | to |
|---|---|---|
| The `-o` extension pin ruling | STAGE-037 | STAGE-050 |
| `watermark` + output format/quality in recipes | STAGE-037 | STAGE-049 |
| Multi-page TIFF / multi-size ICO | STAGE-046 | STAGE-048 |
| Three writing paths still flatten animated input | STAGE-046 | STAGE-048 |
| `info` describes an animated file as a still | STAGE-046 | STAGE-048 |
| **SPEC-118, the shipped-surface conformance matrix** | STAGE-042 | STAGE-050 |

⚠ **SPEC-118 is the duplicate worth naming.** It is a framed spec for a conformance matrix,
parked since 2026-08-15 — and PROJ-011's STAGE-050 plans a `(command × output-flag)` matrix
that is substantially the same instrument. **One of them should absorb the other before either
is built.** STAGE-050's version has a sharper motivation (it would have caught the `-o` pin
divergence), so it is the likelier survivor — but SPEC-118 is already framed, and framing is
the expensive part.

⚠ **STAGE-042 and STAGE-046 cannot themselves move** — both have shipped specs, and a stage
with shipped specs closes in place rather than being re-homed. Only the individual open items
move.

## Keep — actionable, roughly by value

**Correctness, with a named defect:**
- `build` cache HIT swallows the truncated-JPEG warning · `build`'s `Preserve`/`Pinned` arms
  never warn on a truncated JPEG (STAGE-042) — two halves of one gap `apply` does not have.
- The `IMAGE_EXTENSIONS` gap, and `webp` missing from it (STAGE-046) — the tool silently
  processes less than it was handed and reports success. Both marked PRIORITY already.
- `image` 0.25.10's ICO encoder writes a file its own decoder cannot read (STAGE-042) —
  **needs the maintainer's warn / fix / accept before it can be specced**, and it pairs with
  PROJ-011's ICO work.
- Strict JSON `escape_json` (STAGE-036) — SPEC-097's carried tail, a deliberate behaviour fix.

**Corroborated by an external review — two independent sources:**
- **A wasm32 CI leg.** Asked for by both review batches and filed here already. No CI leg runs
  `just wasm-test` today. **Highest-value single item in the set.**
- **Per-operation resource caps.** `check_caps` budgets `w*h*4`; SPEC-122 moved resize to
  `F32x4` at 16 bytes/pixel, so the decode-time cap under-estimates a resize's peak by **4×**.
  ⚠ **Not currently filed as a `- [ ]` anywhere** — it lives only in the feedback capture.
  **File it.**
- Recipes and pipelines are unfuzzed (batch 2). Also unfiled. **File it.**

**Earned by this wave, cheap:**
- `RELEASING.md` step (a), diffing the CHANGELOG against merged specs — **now proven**: running
  it found four missing user-visible entries, not the one it was written for.
- `lock.rs:124-129`'s "same machine" prose · the `/v1` gated-additive-key rule · the
  multi-candidate warning noise · no real 24 MP detail in the corpus · `avif_tile_pin`'s in-test
  cargo build (~+15–25 min CI/PR, 1.3 GB leaked per test process, ships to crates.io).
- `gitignore_files_maybe/` is published to crates.io (STAGE-038) — **a packaging bug, and it
  pairs with SPEC-125 verify's finding that `exclude` does not cover `/tests` either.** Both are
  "we ship files we did not mean to." Do them together.
- `just validate` silently skips untracked files (STAGE-047) — a false green.
- `next_id` mints duplicate spec IDs (STAGE-047) — hit twice now; DEC ids have been reserved
  by hand in every prompt this wave as the workaround.
- `just wasm-size`'s banner mislabels a lean build · the README's `transform` marker gap ·
  CI double-trigger · recurring DCO sign-off failures (STAGE-038).

**Measured, larger:**
- **Decompose `src/cli/optimize.rs`** (STAGE-036) — 1,876 production lines, **grew 9.3% while
  filed**, still the largest in `src/` by 43%. Re-measured 2026-08-21.
- `run_pixel_op` is a serial for-loop, so six batch verbs do not parallelize (STAGE-042).
- `decisions-audit`'s overlap check drowns its own signal — ~1,200 warnings in 2,312 lines,
  401 of them `Cargo.toml ~ Cargo.toml` (STAGE-047).

## Defer or drop — no demand signal, or polish

- **SPEC-092 (`convert --to` rename + social/archive recipes)** — deferred **2026-07-20**, the
  oldest item in the project. STAGE-037's own unpark criterion is *"an adoption signal or a
  maintainer decision to broaden the surface"*, and after a shipped launch **no adoption signal
  has appeared**. ⚠ **This is the item most likely to never be done. Consider cancelling it
  outright rather than carrying it into a third project** — a rename with no demand is churn.
- The `U16x4` working-type probe — **already excluded from 0.7.1 by maintainer ruling**; it is
  an optimization, filed with numbers, and fine where it is.
- The release-lag signal · release-binary-size baseline · `lifetime-report` port · project
  `activity:` field — instrumentation nobody has asked for.
- STAGE-047's framework items: complexity ratings never revisited · prompt budgets in wall-clock
  · squash-merge push stranding · DEC-091/DEC-088 cross-reference · stage-template bullet format.
  ⚠ **These are template feedback, not crustyimg work.** They belong upstream in the template
  repo; carrying them in an app project is why STAGE-047 has nine items and no momentum.

## Cannot be triaged from inside the repo

- **STAGE-041's four items** (publication plan, factual scaffolding, demo asset, pre-publication
  verification). The handoff has said since **2026-08-16** that *three of the four are
  substantially done outside the repo* and that the stage understates reality. **Six days
  unreported.** Nothing here can be assessed until that status lands — and until it does,
  `just backlog` reports four open items that may be one.

---

## What this pass suggests

1. **PROJ-010 does not close yet — and that is fine.** ~24 actionable items is a tail, not a
   graveyard. But it is not a project that "ships"; it is the correctness/quality lane.
2. **Move the six, and resolve SPEC-118 vs STAGE-050 before either is built.**
3. **Three real items are not filed as `- [ ]` anywhere** — the `F32x4` cap gap, unfuzzed
   recipes, and the `/tests` crates.io exclusion. They exist only in `feedback/` and in a
   verify readout. File them or they repeat the `docs/backlog.md` failure.
4. **Cancel SPEC-092** unless a signal appears. It is 33 days old with no demand.
5. **STAGE-047 is template feedback wearing an app-stage costume.** Route it upstream.
